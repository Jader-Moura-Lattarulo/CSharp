namespace M01A05b
{
    class Program
    {
        static void Main(string[] args)
        {
            byte idade = 34;
            ushort publico = 45_239;
            float media = 4.5f; //Sempre que declarar float é necessário a letra f no final do número.
            decimal estrela = 4.059384750m; //Assim como float exige uma declaração "f" ao final do número, o tipo decimal exige "m"
            bool aprovado = false;

            /*
             * Regras:
             * Começar com letra, _ ou @
             * Maiuscula ou Minuscula
             * Só letras, números e _
             * Acentos permitidos
             * Não podemos usar espaços
             * Não pode conter simbolos
             * Não pode ser uma palavra reservada
             */
        }
    }
}